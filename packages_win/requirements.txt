# ============================================================
# pi_agent 폐쇄망 번들 - Python 3.12 오프라인 패키지 요구사항
# 대상: Windows x64, Python 3.12, 관리자 권한 없음
#
# 이 파일은 사람이 읽는 최상위 목록이다(범위 지정, `>=`/`<`). 실제 설치는
# 아래와 같이 정확한 버전을 constraints-py312.txt로 고정한다:
#   pip install --no-index --find-links=packages_win\py312 \
#       --constraint packages_win\constraints-py312.txt \
#       -r packages_win\requirements.txt
#
# 조사 근거: .superpowers/sdd/2026-08-18-pi-agent-closed-network/
#            python-wheelhouse-research.md
# ============================================================

# -- 통계 · 생존분석 (필수 1) --
pandas>=2.1.0,<3.0.0          # lifelines가 <3.0 요구 - 번들 전체의 진짜 상한
numpy>=2.0.0,<2.6.0
scipy>=1.16.0,<1.19.0
statsmodels>=0.14.0,<0.15.0
lifelines>=0.30.0,<0.31.0
matplotlib>=3.10.0,<3.12.0
pyarrow>=24.0.0,<26.0.0

# -- 머신러닝 (필수 2) --
scikit-learn>=1.8.0,<2.0.0
catboost>=1.2.0,<1.3.0
lightgbm>=4.6.0,<4.8.0

# -- 생존분석 보완 / 설명가능성 --
scikit-survival>=0.27.0,<0.29.0   # numba+llvmlite(약 42MB)를 끌어옴 - JIT 컴파일용
shap>=0.50.0,<0.53.0

# -- 시각화 / EDA --
seaborn>=0.13.0,<0.14.0
missingno>=0.5.0,<0.6.0           # 결측 패턴 시각화 - AGENTS.md "결측 처리 방식 명시" 요구와 직결

# -- 데이터 QC --
pandera>=0.30.0,<0.33.0           # pandas DataFrame 스키마 검증, pydantic 기반이라 가볍다

# -- 파일 입출력 --
openpyxl>=3.1.0,<3.2.0
xlsxwriter>=3.2.0,<3.3.0
python-docx>=1.2.0,<1.3.0
pyreadstat>=1.3.0,<1.4.0          # SAS(.sas7bdat/.xpt)/SPSS/Stata 직접 읽기 - AGENTS.md SAS 스택과 연결
tabulate>=0.9.0,<0.11.0

# -- 설정 / 유틸리티 --
tqdm>=4.60.0,<5.0.0
pyyaml>=6.0.0,<7.0.0
python-dotenv>=1.0.0,<2.0.0       # 데이터 경로 환경변수화 - AGENTS.md "경로는 환경변수/상대경로" 요구

# -- 탐색적 분석 (Jupyter) --
jupyterlab>=4.6.0,<4.7.0
notebook>=7.6.0,<7.7.0
ipykernel>=7.3.0,<7.4.0
ipywidgets>=8.1.0,<8.2.0
nbconvert>=7.17.0,<7.18.0
jupytext>=1.19.0,<1.20.0          # .ipynb를 .py/.md 페어링 - git diff 가능, 재현성 요구와 직결
